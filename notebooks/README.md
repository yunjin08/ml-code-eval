# Notebooks

- **`findings.ipynb`** — Loads Phase 3/4 results from `results/` (or `phase3_results/`) and shows primary metrics, hybrid config, McNemar, hypotheses, and bar chart. Run from repo root or from this folder. Requires: `pandas`, `matplotlib`, and `results/phase4_evaluation_report.json` + `phase3_hybrid_config.json` (and optionally `phase3_experiment_results.csv` for the snapshot).
