# Deploy the thesis showcase app to Netlify.
# Build runs from showcase/ only — Netlify never sees Python/requirements.txt.

[build]
  base = "showcase"
  publish = "."
  command = "echo 'Static site — no build'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
